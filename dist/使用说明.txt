1、启动激战2游戏客户端
2、启动本程序，需要以管理员身份运行
3、切换到钓鱼界面
4、鼠标对准水面
5、F10 启动，再次按 F10 关闭

关于替换诱饵
本程序目录旁放置一张 "bait.png" 的图片，即你要自动替换的目标诱饵，确保按 i 能打开背包并且能看到图片里的诱饵。
程序在打开背包后会移动鼠标到诱饵的位置装备诱饵，并不会改变鼠标位置，所以此时关闭背包后鼠标应该扔在水面上，这需要你调整下。

另外程序原理是读内存，然后模拟键盘和鼠标输入，不修改没有魔法，理论上不算外挂，但仍有被封几率，建议知晓~